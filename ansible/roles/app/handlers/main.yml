---
- name: Reload Systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart Flask App
  ansible.builtin.systemd:
    name: "{{ app_name | default('flask') }}" 
    daemon_reload: true
  become: true